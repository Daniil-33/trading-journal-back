# 🚀 Quick Start - Импорт свечных данных

## Что реализовано ✅

1. **Оптимизированная структура данных**: `ohlcv: [Open, High, Low, Close, Volume]` - массив вместо 5 полей
2. **Полный импорт CSV**: парсинг, валидация, batch-импорт с прогрессом
3. **Обработка дубликатов**: автоматический skip через unique constraint
4. **Индексы MongoDB**: compound index `{ pair: 1, timeframe: 1, timestamp: -1 }`

## Быстрый запуск

### 1. Запустите MongoDB
```bash
brew services start mongodb-community
```

### 2. Создайте .env файл
```bash
cp .env.example .env
# Проверьте что MONGODB_URI=mongodb://localhost:27017/macro-analytics
```

### 3. Разместите CSV файлы

Положите CSV файлы в папку `candles-import/` с именами:
- `EURUSD_5m.csv`, `EURUSD_1h.csv`, `EURUSD_1d.csv` и т.д.

Формат CSV:
```csv
Gmt time,Open,High,Low,Close,Volume
2024.01.01 00:00,1.1050,1.1055,1.1048,1.1052,100
```

### 4. Запустите импорт
```bash
npm run build
node dist/integrations/candles/import-candles.js
```

## Поддерживаемые валютные пары
- `EURUSD` - EUR/USD
- `GBPUSD` - GBP/USD  
- `USDJPY` - USD/JPY
- `EURJPY` - EUR/JPY

## Поддерживаемые таймфреймы
- `5m` - 5 минут
- `15m` - 15 минут
- `30m` - 30 минут
- `1h` - 1 час
- `4h` - 4 часа
- `1d` - 1 день
- `1w` - 1 неделя

## Особенности импорта

✅ **Batch processing** - 10,000 свечей за раз  
✅ **Validation** - проверка OHLC отношений  
✅ **Duplicate handling** - автоматически пропускает дубликаты  
✅ **Progress tracking** - детальная статистика в реальном времени  
✅ **Error recovery** - продолжает работу при ошибках  

## Проверка результата

```bash
# Подключитесь к MongoDB
mongosh

# Выберите БД
use macro-analytics

# Количество свечей
db.candles.countDocuments()

# Примеры данных
db.candles.find().limit(5)

# Статистика
db.candles.aggregate([
  { $group: { 
      _id: { pair: "$pair", timeframe: "$timeframe" },
      count: { $sum: 1 }
  }}
])
```

## Полная документация

См. [CANDLE-IMPORT.md](./CANDLE-IMPORT.md) для подробной информации.

---

**Готово к импорту 20 лет исторических данных!** 🎉
