# 📚 Документация по развертыванию MongoDB + mongo-express

## 📋 Содержание

1. [Описание](#описание)
2. [Требования](#требования)
3. [Быстрый старт](#быстрый-старт)
4. [Подробная инструкция](#подробная-инструкция)
5. [Структура проекта](#структура-проекта)
6. [Конфигурация](#конфигурация)
7. [Отдельные скрипты](#отдельные-скрипты)
8. [Управление системой](#управление-системой)
9. [Резервное копирование](#резервное-копирование)
10. [Устранение неполадок](#устранение-неполадок)
11. [Безопасность](#безопасность)

---

## 📖 Описание

Этот набор скриптов автоматизирует полное развертывание MongoDB с веб-интерфейсом mongo-express на Ubuntu/Debian VPS сервере.

### Что устанавливается:

- **MongoDB 7.0** - NoSQL база данных
- **mongo-express** - веб-интерфейс для управления MongoDB (в Docker контейнере)
- **Nginx** - reverse proxy для mongo-express
- **Let's Encrypt SSL** - бесплатный SSL сертификат
- **Docker & Docker Compose** - для запуска mongo-express
- **Certbot** - для автоматического управления SSL сертификатами

---

## ⚙️ Требования

### Минимальные требования к серверу:

- **ОС:** Ubuntu 20.04+ или Debian 11+
- **RAM:** 1 GB (рекомендуется 2 GB)
- **Диск:** 10 GB свободного места
- **CPU:** 1 ядро (рекомендуется 2)
- **Доступ:** root или sudo пользователь

### Перед началом:

1. ✅ Имеете VPS сервер с Ubuntu/Debian
2. ✅ Имеете доступ по SSH к серверу
3. ✅ Имеете зарегистрированный домен
4. ✅ Можете настроить DNS записи для домена

---

## 🚀 Быстрый старт

### Шаг 1: Настройка DNS

Создайте A-запись в панели управления вашим доменом:

```
Type: A
Name: db (или ваш поддомен)
Value: [IP вашего VPS]
TTL: Auto
```

**Подождите 10-30 минут** для распространения DNS.

### Шаг 2: Скопируйте скрипты на сервер

```bash
# С вашего локального компьютера
scp -r deployment-scripts/ root@your-server-ip:~/
```

### Шаг 3: Настройте конфигурацию

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Перейдите в директорию
cd ~/deployment-scripts

# Отредактируйте конфигурацию
nano config.sh
```

Измените следующие параметры:
- `SERVER_IP` - IP адрес вашего сервера
- `DOMAIN` - ваш домен для mongo-express
- `EMAIL` - ваш email для SSL
- `MONGO_USER` - имя пользователя MongoDB
- `MONGO_PASSWORD` - пароль пользователя MongoDB
- `ADMIN_USER` - логин для веб-интерфейса
- `ADMIN_PASSWORD` - пароль для веб-интерфейса

### Шаг 4: Запустите установку

```bash
# Сделайте скрипты исполняемыми
chmod +x *.sh

# Запустите полную установку
./deploy.sh
```

Скрипт автоматически:
1. Установит все зависимости
2. Настроит MongoDB
3. Развернет mongo-express
4. Настроит Nginx
5. Установит SSL сертификат

### Шаг 5: Готово!

Откройте в браузере: `https://ваш-домен`

Войдите с учетными данными из `config.sh`.

---

## 📚 Подробная инструкция

### 1. Подготовка сервера

#### 1.1 Подключение к серверу

```bash
ssh root@your-server-ip
# или
ssh your-user@your-server-ip
```

#### 1.2 Обновление системы (опционально, но рекомендуется)

```bash
sudo apt-get update
sudo apt-get upgrade -y
```

#### 1.3 Копирование скриптов

**Вариант А: Через SCP**

```bash
# С вашего компьютера
scp -r deployment-scripts/ root@your-server-ip:~/
```

**Вариант Б: Через Git**

```bash
# На сервере
cd ~
git clone [your-repo-url]
cd deployment-scripts
```

**Вариант В: Вручную**

```bash
# На сервере
mkdir -p ~/deployment-scripts
cd ~/deployment-scripts

# Затем скопируйте содержимое файлов вручную
```

### 2. Настройка конфигурации

#### 2.1 Редактирование config.sh

```bash
cd ~/deployment-scripts
nano config.sh
```

#### 2.2 Основные параметры

```bash
# IP адрес вашего VPS сервера
SERVER_IP="123.45.67.89"

# Домен для mongo-express (например: db.example.com)
DOMAIN="db.example.com"

# Email для уведомлений Let's Encrypt
EMAIL="admin@example.com"

# Учетные данные MongoDB
MONGO_USER="admin"
MONGO_PASSWORD="your-strong-password"

# Учетные данные для веб-интерфейса
ADMIN_USER="admin"
ADMIN_PASSWORD="your-web-password"
```

💡 **Совет:** Используйте разные пароли для MongoDB и веб-интерфейса!

🔒 **Безопасность:** Используйте сложные пароли (минимум 12 символов, буквы, цифры, спецсимволы)

### 3. Полная установка

#### 3.1 Запуск deploy.sh

```bash
# Сделать скрипты исполняемыми
chmod +x *.sh

# Запустить полную установку
./deploy.sh
```

#### 3.2 Что происходит во время установки

Скрипт выполнит 5 шагов:

**Шаг 1: Установка зависимостей** (~5-10 минут)
- MongoDB 7.0
- Docker и Docker Compose
- Nginx
- Certbot

**Шаг 2: Настройка MongoDB** (~2 минуты)
- Настройка конфигурации
- Создание пользователя
- Включение авторизации

**Шаг 3: Развертывание mongo-express** (~1 минута)
- Создание docker-compose.yml
- Запуск контейнера

**Шаг 4: Настройка Nginx** (~1 минута)
- Создание конфигурации
- Настройка reverse proxy

**Шаг 5: Установка SSL** (~2 минуты)
- Получение сертификата от Let's Encrypt
- Настройка HTTPS
- Автоматическое перенаправление HTTP → HTTPS

**Общее время:** 10-15 минут

### 4. Проверка установки

После завершения установки проверьте:

```bash
# Статус MongoDB
sudo systemctl status mongod

# Статус mongo-express
docker ps | grep mongo-express

# Статус Nginx
sudo systemctl status nginx

# Проверка SSL
sudo certbot certificates
```

### 5. Первый вход

1. Откройте браузер
2. Перейдите на `https://ваш-домен`
3. Введите учетные данные из `config.sh`
4. Готово! Вы в интерфейсе управления MongoDB

---

## 📁 Структура проекта

```
deployment-scripts/
│
├── config.sh                    # Конфигурация (РЕДАКТИРУЙТЕ ЭТО!)
│
├── deploy.sh                    # Главный скрипт полной установки
│
├── install-dependencies.sh      # Установка MongoDB, Docker, Nginx, Certbot
│
├── setup-mongodb.sh             # Настройка MongoDB и создание пользователя
│
├── setup-mongo-express.sh       # Развертывание mongo-express в Docker
│
├── setup-nginx.sh               # Настройка Nginx как reverse proxy
│
├── setup-ssl.sh                 # Установка SSL сертификата
│
├── uninstall.sh                 # Удаление всей установки
│
└── README.md                    # Эта документация
```

---

## ⚙️ Конфигурация

### config.sh - Основной файл конфигурации

#### Параметры сервера

```bash
SERVER_IP="46.62.248.7"         # IP адрес VPS
SSH_USER="root"                  # Пользователь SSH
```

#### Параметры домена

```bash
DOMAIN="db.example.com"          # Домен для mongo-express
EMAIL="admin@example.com"        # Email для SSL
```

#### Параметры MongoDB

```bash
MONGO_USER="admin"               # Пользователь MongoDB
MONGO_PASSWORD="password"        # Пароль MongoDB
MONGO_PORT="27017"               # Порт MongoDB
```

#### Параметры mongo-express

```bash
ADMIN_USER="admin"               # Логин веб-интерфейса
ADMIN_PASSWORD="password"        # Пароль веб-интерфейса
MONGO_EXPRESS_PORT="8081"        # Порт mongo-express
```

#### Параметры Nginx

```bash
NGINX_HTTP_PORT="80"             # HTTP порт
NGINX_HTTPS_PORT="443"           # HTTPS порт
```

#### Версии ПО

```bash
MONGODB_VERSION="7.0"            # Версия MongoDB
MONGO_EXPRESS_VERSION="latest"   # Версия mongo-express
```

---

## 🔧 Отдельные скрипты

Вы можете запускать скрипты по отдельности для частичной установки или переустановки компонентов.

### 1. install-dependencies.sh

**Назначение:** Установка всех зависимостей

**Когда использовать:**
- Первая установка на чистый сервер
- Обновление версий компонентов

**Запуск:**
```bash
source config.sh
./install-dependencies.sh
```

**Что устанавливает:**
- MongoDB 7.0
- Docker и Docker Compose
- Nginx
- Certbot
- Базовые утилиты

---

### 2. setup-mongodb.sh

**Назначение:** Настройка MongoDB и создание пользователя

**Когда использовать:**
- После установки MongoDB
- Для создания нового пользователя
- Для изменения настроек MongoDB

**Запуск:**
```bash
source config.sh
./setup-mongodb.sh
```

**Что делает:**
- Настраивает MongoDB для прослушивания на всех интерфейсах
- Создает пользователя с правами root
- Включает авторизацию
- Проверяет работоспособность

**Важно:** Скрипт безопасен для повторного запуска

---

### 3. setup-mongo-express.sh

**Назначение:** Развертывание mongo-express

**Когда использовать:**
- Первая установка mongo-express
- Обновление mongo-express
- Изменение настроек (пароль, порт)

**Запуск:**
```bash
source config.sh
./setup-mongo-express.sh
```

**Что делает:**
- Создает `/opt/mongo-express/docker-compose.yml`
- Запускает контейнер Docker
- Настраивает подключение к MongoDB
- Настраивает авторизацию веб-интерфейса

**Расположение файлов:**
- docker-compose.yml: `/opt/mongo-express/`
- Логи: `docker logs mongo-express`

---

### 4. setup-nginx.sh

**Назначение:** Настройка Nginx как reverse proxy

**Когда использовать:**
- Первая настройка Nginx
- Изменение домена
- Обновление конфигурации

**Запуск:**
```bash
source config.sh
./setup-nginx.sh
```

**Что делает:**
- Создает конфигурацию для домена
- Настраивает reverse proxy на mongo-express
- Настраивает заголовки безопасности
- Перезапускает Nginx

**Расположение файлов:**
- Конфигурация: `/etc/nginx/sites-available/${DOMAIN}`
- Логи: `/var/log/nginx/${DOMAIN}_*.log`

---

### 5. setup-ssl.sh

**Назначение:** Установка SSL сертификата

**Когда использовать:**
- После настройки Nginx
- После распространения DNS
- Для обновления сертификата

**Запуск:**
```bash
source config.sh
./setup-ssl.sh
```

**Что делает:**
- Проверяет DNS
- Получает сертификат от Let's Encrypt
- Настраивает HTTPS
- Настраивает автоматическое перенаправление HTTP → HTTPS
- Настраивает автопродление

**Требования:**
- DNS должен быть настроен
- Порты 80 и 443 должны быть открыты
- Домен должен указывать на сервер

---

### 6. uninstall.sh

**Назначение:** Полное удаление установки

**⚠️ ОПАСНО:** Удаляет все данные MongoDB!

**Когда использовать:**
- Полная переустановка
- Удаление системы

**Запуск:**
```bash
source config.sh
./uninstall.sh
```

**Что удаляет:**
- MongoDB (включая все базы данных!)
- mongo-express контейнер
- Nginx конфигурацию
- SSL сертификаты

**Что остается:**
- Docker
- Nginx (программа)
- Certbot

---

## 🎮 Управление системой

### MongoDB

#### Статус и управление

```bash
# Статус
sudo systemctl status mongod

# Запуск
sudo systemctl start mongod

# Остановка
sudo systemctl stop mongod

# Перезапуск
sudo systemctl restart mongod

# Автозапуск
sudo systemctl enable mongod
```

#### Подключение к MongoDB

```bash
# Через командную строку
mongosh admin -u your-user -p your-password

# Проверка подключения
mongosh admin -u your-user -p your-password --eval "db.runCommand({ connectionStatus: 1 })"
```

#### Логи MongoDB

```bash
# Логи через journalctl
sudo journalctl -u mongod -f

# Последние 50 строк
sudo journalctl -u mongod -n 50

# Логи из файла
sudo tail -f /var/log/mongodb/mongod.log
```

#### Конфигурация MongoDB

```bash
# Редактирование конфигурации
sudo nano /etc/mongod.conf

# После изменений перезапустите
sudo systemctl restart mongod
```

---

### mongo-express

#### Статус контейнера

```bash
# Список запущенных контейнеров
docker ps | grep mongo-express

# Подробная информация
docker inspect mongo-express
```

#### Управление контейнером

```bash
# Перейти в директорию
cd /opt/mongo-express

# Запуск
docker compose up -d

# Остановка
docker compose down

# Перезапуск
docker compose restart

# Пересоздание (после изменения config)
docker compose up -d --force-recreate
```

#### Логи mongo-express

```bash
# Просмотр логов в реальном времени
docker logs mongo-express -f

# Последние 50 строк
docker logs mongo-express --tail 50

# Логи с временными метками
docker logs mongo-express -f --timestamps
```

#### Обновление mongo-express

```bash
cd /opt/mongo-express

# Остановка
docker compose down

# Обновление образа
docker pull mongo-express:latest

# Запуск с новым образом
docker compose up -d
```

---

### Nginx

#### Статус и управление

```bash
# Статус
sudo systemctl status nginx

# Запуск
sudo systemctl start nginx

# Остановка
sudo systemctl stop nginx

# Перезапуск
sudo systemctl restart nginx

# Перезагрузка конфигурации (без остановки)
sudo systemctl reload nginx

# Проверка конфигурации
sudo nginx -t
```

#### Логи Nginx

```bash
# Логи для вашего домена
sudo tail -f /var/log/nginx/your-domain_access.log
sudo tail -f /var/log/nginx/your-domain_error.log

# Общие логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

#### Редактирование конфигурации

```bash
# Конфигурация сайта
sudo nano /etc/nginx/sites-available/your-domain

# После изменений
sudo nginx -t
sudo systemctl reload nginx
```

---

### SSL Сертификаты

#### Информация о сертификатах

```bash
# Список сертификатов
sudo certbot certificates

# Информация о конкретном сертификате
sudo certbot certificates -d your-domain
```

#### Обновление сертификатов

```bash
# Тестовое обновление
sudo certbot renew --dry-run

# Принудительное обновление
sudo certbot renew --force-renewal

# Обновление конкретного сертификата
sudo certbot renew --cert-name your-domain
```

#### Удаление сертификата

```bash
sudo certbot delete --cert-name your-domain
```

#### Автоматическое обновление

```bash
# Проверка таймера автообновления
sudo systemctl status certbot.timer

# Запуск таймера
sudo systemctl enable certbot.timer
sudo systemctl start certbot.timer
```

---

## 💾 Резервное копирование

### Резервное копирование MongoDB

#### Полный бэкап всех баз

```bash
# Создание бэкапа
mongodump \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  --out=/backup/mongodb/$(date +%Y%m%d)

# С сжатием
mongodump \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  --archive=/backup/mongodb/backup-$(date +%Y%m%d).gz \
  --gzip
```

#### Бэкап конкретной базы

```bash
mongodump \
  --uri="mongodb://your-user:your-password@localhost:27017/your-database?authSource=admin" \
  --out=/backup/mongodb/your-database
```

#### Восстановление из бэкапа

```bash
# Восстановление всех баз
mongorestore \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  /backup/mongodb/20240101

# Восстановление из архива
mongorestore \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  --archive=/backup/mongodb/backup-20240101.gz \
  --gzip

# Восстановление конкретной базы
mongorestore \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  --db=your-database \
  /backup/mongodb/your-database
```

### Автоматизация бэкапов

#### Создание скрипта резервного копирования

```bash
#!/bin/bash
# /root/mongodb-backup.sh

BACKUP_DIR="/backup/mongodb"
RETENTION_DAYS=7

# Создание бэкапа
mongodump \
  --uri="mongodb://your-user:your-password@localhost:27017/?authSource=admin" \
  --archive=${BACKUP_DIR}/backup-$(date +%Y%m%d).gz \
  --gzip

# Удаление старых бэкапов
find ${BACKUP_DIR} -name "backup-*.gz" -mtime +${RETENTION_DAYS} -delete

echo "Backup completed: $(date)"
```

#### Настройка cron

```bash
# Редактирование crontab
crontab -e

# Добавить строку для ежедневного бэкапа в 2:00
0 2 * * * /root/mongodb-backup.sh >> /var/log/mongodb-backup.log 2>&1
```

---

## 🔍 Устранение неполадок

### MongoDB не запускается

**Проблема:** `sudo systemctl start mongod` завершается с ошибкой

**Решение:**

1. Проверьте логи:
```bash
sudo journalctl -u mongod -n 100
```

2. Проверьте конфигурацию:
```bash
sudo mongod --config /etc/mongod.conf --port 27018
```

3. Проверьте права на файлы:
```bash
sudo chown -R mongodb:mongodb /var/lib/mongodb
sudo chown -R mongodb:mongodb /var/log/mongodb
```

4. Проверьте место на диске:
```bash
df -h
```

---

### mongo-express не подключается к MongoDB

**Проблема:** В логах `docker logs mongo-express` ошибки подключения

**Решение:**

1. Проверьте, что MongoDB запущен:
```bash
sudo systemctl status mongod
```

2. Проверьте, что MongoDB слушает на 0.0.0.0:
```bash
sudo netstat -tlnp | grep 27017
```

Должно быть: `0.0.0.0:27017`

Если `127.0.0.1:27017`, исправьте в `/etc/mongod.conf`:
```yaml
net:
  bindIp: 0.0.0.0
```

3. Проверьте учетные данные в docker-compose.yml:
```bash
cat /opt/mongo-express/docker-compose.yml
```

4. Перезапустите контейнер:
```bash
cd /opt/mongo-express
docker compose down
docker compose up -d
docker logs mongo-express -f
```

---

### Nginx 502 Bad Gateway

**Проблема:** При открытии сайта ошибка 502

**Решение:**

1. Проверьте, что mongo-express работает:
```bash
docker ps | grep mongo-express
curl http://localhost:8081
```

2. Проверьте логи Nginx:
```bash
sudo tail -f /var/log/nginx/your-domain_error.log
```

3. Проверьте конфигурацию Nginx:
```bash
sudo nginx -t
```

4. Проверьте, что порт правильный в конфигурации:
```bash
cat /etc/nginx/sites-available/your-domain | grep proxy_pass
# Должно быть: proxy_pass http://127.0.0.1:8081;
```

---

### SSL сертификат не устанавливается

**Проблема:** Certbot выдает ошибку при установке SSL

**Решение:**

1. Проверьте DNS:
```bash
dig @8.8.8.8 your-domain
nslookup your-domain
```

Домен должен возвращать IP вашего сервера.

2. Проверьте, что порт 80 доступен извне:
```bash
curl -I http://your-domain
```

3. Проверьте файрвол:
```bash
sudo ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

4. Проверьте логи Certbot:
```bash
sudo cat /var/log/letsencrypt/letsencrypt.log
```

5. Попробуйте ручной режим:
```bash
sudo certbot --nginx -d your-domain --debug
```

---

### Забыли пароль

**Проблема:** Забыли пароль MongoDB или mongo-express

**Решение для MongoDB:**

1. Временно отключите авторизацию:
```bash
sudo nano /etc/mongod.conf
# Измените authorization: enabled на authorization: disabled
sudo systemctl restart mongod
```

2. Смените пароль:
```bash
mongosh admin
db.updateUser("your-user", { pwd: "new-password" })
exit
```

3. Включите авторизацию обратно:
```bash
sudo nano /etc/mongod.conf
# Измените authorization: disabled на authorization: enabled
sudo systemctl restart mongod
```

**Решение для mongo-express:**

1. Отредактируйте docker-compose.yml:
```bash
sudo nano /opt/mongo-express/docker-compose.yml
# Измените ME_CONFIG_BASICAUTH_PASSWORD
```

2. Перезапустите контейнер:
```bash
cd /opt/mongo-express
docker compose down
docker compose up -d
```

---

### Проблемы с производительностью

**Проблема:** Медленная работа MongoDB или mongo-express

**Решение:**

1. Проверьте ресурсы сервера:
```bash
top
free -h
df -h
```

2. Проверьте использование ресурсов MongoDB:
```bash
mongosh admin -u your-user -p your-password
db.serverStatus()
db.currentOp()
```

3. Увеличьте ресурсы контейнера mongo-express:
```yaml
# В docker-compose.yml
deploy:
  resources:
    limits:
      memory: 1G
```

4. Настройте индексы в MongoDB:
```bash
mongosh your-database -u your-user -p your-password
db.your-collection.createIndex({ field: 1 })
```

---

## 🔒 Безопасность

### Базовые рекомендации

#### 1. Используйте сложные пароли

```bash
# Генерация случайного пароля
openssl rand -base64 32
```

#### 2. Настройте файрвол

```bash
# Разрешите только необходимые порты
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable
```

#### 3. Ограничьте доступ к MongoDB

По умолчанию MongoDB доступен только с localhost.

Если нужен внешний доступ:

```bash
# В /etc/mongod.conf
net:
  bindIp: 0.0.0.0

# НО! Откройте порт только для нужных IP
sudo ufw allow from YOUR_IP to any port 27017
```

#### 4. Регулярно обновляйте систему

```bash
sudo apt-get update
sudo apt-get upgrade -y
```

#### 5. Настройте регулярные бэкапы

См. раздел [Резервное копирование](#резервное-копирование)

#### 6. Мониторинг логов

```bash
# Настройте логротацию
sudo nano /etc/logrotate.d/mongodb

# Добавьте
/var/log/mongodb/*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0600 mongodb mongodb
}
```

#### 7. Ограничьте доступ к mongo-express по IP

В конфигурации Nginx:

```nginx
location / {
    # Разрешить только с вашего IP
    allow YOUR_IP;
    deny all;
    
    proxy_pass http://127.0.0.1:8081;
    # ... остальные настройки
}
```

#### 8. Используйте SSH ключи вместо паролей

```bash
# На вашем компьютере
ssh-keygen -t rsa -b 4096

# Скопировать ключ на сервер
ssh-copy-id root@your-server-ip

# На сервере отключить авторизацию по паролю
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no
sudo systemctl restart sshd
```

---

## 📞 Поддержка

### Полезные ресурсы

- [MongoDB Documentation](https://docs.mongodb.com/)
- [mongo-express GitHub](https://github.com/mongo-express/mongo-express)
- [Nginx Documentation](https://nginx.org/ru/docs/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)

### Логи для отладки

При возникновении проблем соберите следующую информацию:

```bash
# Информация о системе
uname -a
cat /etc/os-release

# Статусы сервисов
sudo systemctl status mongod
sudo systemctl status nginx
docker ps -a

# Логи
sudo journalctl -u mongod -n 50
sudo tail -50 /var/log/nginx/error.log
docker logs mongo-express --tail 50

# Порты
sudo netstat -tlnp
```

---

## 🎓 Заключение

Этот набор скриптов позволяет быстро развернуть MongoDB с веб-интерфейсом на любом новом сервере. 

**Ключевые преимущества:**
- ✅ Полная автоматизация
- ✅ Безопасная конфигурация по умолчанию
- ✅ SSL из коробки
- ✅ Легкое управление
- ✅ Простая миграция между серверами

**Время развертывания:** 10-15 минут

**Необходимые действия вручную:**
1. Настройка DNS (1 раз)
2. Редактирование config.sh (1 раз)
3. Запуск ./deploy.sh (1 команда)

Удачного развертывания! 🚀
